# 作業計画書テンプレート（GCP / MLOps / Terraform）

作業計画書の記入雛形。ゲート①提出物として使う。上から順に埋める。
コードサンプルは含めない（方針・地図に徹する）。

> 参照: [01_workflow-guide.md](01_workflow-guide.md) の4つの柱 / [02_copilot-prompts.md](02_copilot-prompts.md) の Step 4 プロンプト

---

## このテンプレートの使い方

**🔴 必須セクション**はタスク種別・規模に関わらず必ず記入する。

**🟡 条件付きセクション**は記入条件を満たす場合のみ記入する。該当しない場合は `（このタスクでは不要）` と一行書いて次に進む。

| セクション | 種別 | 記入条件 |
|---|---|---|
| タスク種別 / ゴール / 成功定義 / 共通認識 | 🔴 必須 | 常に |
| 実装方針 | 🔴 必須 | 常に |
| AI提案の実在確認 | 🔴 必須 | 常に |
| 変更/新規対象ファイル | 🟡 条件 | ファイル変更がある場合（Terraform リソース名も含む） |
| 影響範囲 | 🟡 条件 | 既存パイプライン / 他バッチ / 共有テーブルへの影響がある場合 |
| テスト方針 | 🟡 条件 | Cloud Run Job / Vertex AI Pipeline のみ（Elastic Search・Terraform は成功定義で代替） |
| リスク | 🟡 条件 | 本番データ変更 / 共有リソース / 大量データ / 初めての技術領域 |
| 確認事項 | 🟡 条件 | リーダー/クライアントへの確認待ち事項がある場合 |
| コミット計画 | 🟡 条件 | 複数コミットが必要な変更（単一論理変更なら不要） |

---

## 🔴 タスク種別
Cloud Run Job（Pythonバッチ）/ Vertex AI Pipeline / Elastic Search / Terraform / その他（該当を残す）

## 🔴 タスクゴール
<!-- 達成したいこと。技術的な手段ではなく「何が変わるか」を1〜3文で -->
〈例〉Feature Store の特徴量を日次バッチで更新し、推論パイプラインが最新値を参照できるようにする

## 🔴 成功定義

<!-- タスク種別に合ったテーブルを残し、他を削除して埋める -->

### Cloud Run Job（Pythonバッチ）の場合

| 成功条件 | 確認方法 |
|---|---|
| ジョブが正常終了する | Cloud Run Job の exit code 0 + Cloud Logging でエラーなし |
| 処理件数がログで追跡できる | Cloud Logging に処理件数・完了メッセージあり |
| BigQuery / GCS への出力が仕様と一致する | 出力テーブルの行数・値・スキーマを確認（ログ確認だけでは不十分） |
| 再実行しても二重書き込みが起きない | 2回実行して行数・レコードが変わらないことを確認 |

> ログ確認は「処理が完了したか」の確認。「正しい結果が出たか」は作成物（BQ テーブル / GCS ファイル / Feature Store 値）のチェックが必要。両方を成功定義に含める。

### Vertex AI Pipeline の場合（テストコードは限定的）

| 成功条件 | 確認方法 |
|---|---|
| 全コンポーネントが正常完了する | Vertex AI コンソールでパイプライン実行ステータスを確認 |
| 各コンポーネントの出力アーティファクトが正しい | GCS / Vertex AI Experiments で中間出力を確認 |
| Feature Store に正しい値が書き込まれている | Feature Store API または BQ エクスポートで値を確認 |
| BQ 出力テーブルが仕様の形式・件数と一致する | クエリで行数・値サンプルを確認 |

### Elastic Search の場合（テストコードは書かない）

| 成功条件 | 確認方法 |
|---|---|
| インデックスのドキュメント数が期待通り | `_count` API で確認 |
| クエリのレスポンスが仕様の形式と一致する | 代表的なクエリを実行してレスポンスを目視確認 |
| 既存インデックスへの意図しない影響がない | 別インデックスのドキュメント数・クエリ動作を確認 |

### Terraform の場合（テストコードは書かない）

| 成功条件 | 確認方法 |
|---|---|
| 構文エラーがない | `terraform validate` が通る |
| 変更内容が意図通り | `terraform plan` の差分を目視確認 |
| 既存 GCP リソースへの意図しない変更がない | plan 出力の destroy/change 件数を確認 |
| apply 後にリソースが期待状態 | Google Cloud コンソールまたは `gcloud` コマンドで確認 |

## 🔴 リーダー・メンバー共通認識
- スコープに含まれること：
- スコープに含まれないこと：
- 判断が難しいケース：（リーダー確認済みのものは回答を記載）

---

## 🔴 実装方針
<!-- 処理の流れを箇条書き。「何をどの順序でどこに書くか」の地図 -->
-

---

## 🟡 変更 / 新規対象ファイル
> 📌 条件: ファイル変更がある場合のみ（Terraform の場合はリソース名を記載）

| パス | 変更種別 | 概要 |
|---|---|---|
|  | 新規 / 変更 |  |

## 🟡 影響範囲
> 📌 条件: 既存パイプライン / 他チームのバッチ / 共有テーブルへの影響がある場合のみ

-

## 🟡 テスト方針
> 📌 条件: Cloud Run Job / Vertex AI Pipeline のみ。Elastic Search・Terraform は成功定義の確認方法で代替するため不要

-

## 🟡 リスク
> 📌 条件: 本番データ変更 / 共有リソース / 大量データ処理 / 初めての技術領域 のいずれかに該当する場合

-

## 🟡 確認事項
> 📌 条件: リーダーまたはクライアントへの確認待ちがある場合のみ

-

## 🟡 コミット計画
> 📌 条件: 複数コミットが必要な変更のみ（単一論理変更なら不要）

-

---

## 🔴 AI提案の実在確認

| 対象 | 種別 | 確認方法 | 結果 | 根拠 |
|---|---|---|---|---|
|  | 関数 / API / ライブラリ / 設定キー / DB カラム | rg / import / schema / 公式doc / test | 既存あり / 新規作成 / 要検証 / 採用不可 | ファイル・コマンド結果・URL |

> `[要検証]` を確定事項として扱わない。残る場合は確認不能な理由・確認先・作業への影響を明記する。

---

# 付録: 実装サンプルメモ（メンバー用の実装補助メモ）

**誰から誰へ**: メンバー → メンバー自身（リーダーへの提出物ではない）
**誰が作成する**: メンバー
**誰が使う**: メンバーが自分の実装補助として使う

**このメモについての誤解を防ぐため、以下を明確にする:**

| よくある誤解 | 実際 |
|---|---|
| リーダーに提出する資料 | 提出しない |
| 実装前にコードを書いてよい | 原則、完成コードは書かない |
| 作業計画書の一部 | 必須ではない。自分用の補助資料 |
| Copilot に作らせたコード案 | 方向性確認用の断片に限定 |
| レビュー対象 | 原則レビュー対象外 |

> 実装サンプルメモは、メンバーが自分の理解整理のために作成する**任意資料**である。
> リーダーへの提出物ではない。目的は実装時の迷いを減らすことであり、実装を先行することではない。
> 記載できるのは、新規・変更コードの重要箇所の抜粋・疑似コード・設定例・SQL例などの断片に限る。
> **完成コード全文や既存コードの長文貼り付けは禁止する。**

## 目指すシグネチャと責務

```python
# 〈例〉新規関数の入出力の型と責務を一言
def process_features(entity_ids: list[str], as_of: date) -> pd.DataFrame:
    """Feature Store から特徴量を取得して返す。"""
    ...
```

## 処理の要となる箇所（抜粋）

```python
# 冪等性・チャンク処理・異常系分岐など、設計判断が必要な箇所だけ抜粋
for chunk in chunked(entity_ids, size=500):
    ...
```

## 既存コードとの接続点

```
〈例〉
- src/batch/main.py の main() から呼び出す
- src/gcp/bq_client.py の BigQueryClient を再利用する
- Cloud Logging には既存の setup_logger() を使う
```
